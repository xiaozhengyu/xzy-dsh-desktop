{
  "name": "dsh-desktop",
  "private": true,
  "version": "0.1.0",
  "description": "DeepSeek Harness 桌面端 —— Tauri 2 外壳（Windows 11，不内嵌 Node.js，调用系统全局 node/dsh）",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:portable": "tauri build --no-bundle",
    "tauri": "tauri",
    "icons": "tauri icon assets/app-icon.png",
    "dev:front": "node scripts/frontend.mjs --serve",
    "build:front": "node scripts/frontend.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
